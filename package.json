{
  "name": "markdown-web-clipper",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Tooling for the Markdown Web Clipper Chrome extension (tests + lint + vendored libraries). The extension itself ships from extension/.",
  "scripts": {
    "test": "node --test",
    "lint": "eslint extension/src",
    "vendor": "node scripts/vendor-libs.mjs"
  },
  "devDependencies": {
    "@mozilla/readability": "^0.6.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "jsdom": "^25.0.0",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2"
  }
}
